[build]
jobs = 4
# No target-dir here on purpose: the global ~/.cargo/config.toml already points
# at the shared D:/cargo-target, and a committed absolute path broke the 0.2.53
# release on all four platforms (CI wrote its bundle outside the staged path).

[env]
RUST_TEST_THREADS = "4"
