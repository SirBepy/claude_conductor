[build]
jobs = 4
# Own D: subfolder so this project's build artifacts never collide with other
# projects sharing the global target-dir (see ~/.cargo/config.toml comment).
target-dir = "D:/cargo-target/claude_usage_in_taskbar__src-tauri"

[env]
RUST_TEST_THREADS = "4"
